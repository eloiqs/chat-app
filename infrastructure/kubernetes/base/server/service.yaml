apiVersion: v1
kind: Service
metadata:
  name: server
  labels:
    app: chatapp
    component: server
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: chatapp
    component: server
