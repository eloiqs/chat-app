apiVersion: v1
kind: ServiceAccount
metadata:
  name: migrations
  labels:
    app: chatapp
    component: migrations
  annotations:
    # This annotation is set per-environment to link to IRSA role
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/chatapp-ENV-migrations-role
