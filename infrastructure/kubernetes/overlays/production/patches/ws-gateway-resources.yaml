apiVersion: apps/v1
kind: Deployment
metadata:
  name: ws-gateway
spec:
  template:
    spec:
      containers:
        - name: ws-gateway
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
